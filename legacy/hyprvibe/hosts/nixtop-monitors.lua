-- Start generic during migration. Replace with detected output names/layout after
-- `hyprctl monitors all` has been captured on nixtop.
hl.monitor({ output = "", mode = "preferred", position = "auto", scale = 1 })
